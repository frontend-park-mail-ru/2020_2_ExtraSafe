language: node_js

node_js:
  - '12'

services:
  - docker

addons:
  - ssh_known_hosts: 95.163.213.142

install:
  - npm ci

script:
  - npm run build

deploy:
  provider: script
  script: bash deploy.sh
  on:
    branch: cicd
